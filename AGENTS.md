# Comfy-ops 项目约定（所有会话自动加载）

## 内容落盘决策树（写任何内容前套用）

```
这条信息是？
├─ 稳定、可执行、需要查的参考（模型清单/工作流结构/参数表/安装要点）→ docs/ + .pi/skills/comfyui/（手册）
├─ 动态经验、踩坑、实测数据、对比结论、用户偏好 → Mem0 共享记忆（memory_retain，user_id=comfy-ops）
├─ 会话级进度（正在做什么/交接下一步）→ docs/05_session_handoff.md（短期，收尾归档）
└─ 拿不准 → 问用户
```

## 记忆纪律（Mem0 共享记忆）

- **会话开始**：先 `memory_recall` 检索相关经验（关键词如 "Bernini"、"超分"、"踩坑"）
- **会话结束**：`memory_retain` 本会话的关键经验（实测数字、新踩的坑、新认知）
- **手册 vs 经验**：可执行参考进文档/SKILL；经验性内容进 Mem0——别都堆进文档
- 文档越写越大的时候，先问：这算经验还是手册？经验 → Mem0

## 写协作纪律（多 Agent）

- 写共享文件（SKILL/docs/工作流）前先 read 最新版，基于最新增量改
- SKILL.md / docs 单一写者；要并行写就开独立文件
- 每会话结束 git commit（改动可回滚）

## 网络（用户权威配置）

- Clash Rule 模式 + TUN 接管（无 system proxy）：GitHub 走代理节点、国内直连
- 默认直连即可；显式 `-x http://127.0.0.1:7890` 仅作失败兜底
- **遇到网络异常，第一时间与用户确认和测试，不擅自改配置/反复重试**
- HF 下载用 hf-mirror.com（直连）；pip 用清华源
